{
  "name": "magnificat",
  "version": "0.0.1",
  "description": "Magnificent cataloguing app for classical music enthusiasts",
  "main": "./main.js",
  "scripts": {
    "start": "electron . built",
    "plain": "electron . plain",
    "package": "electron-packager ./ mcat --ignore=client$ --ignore=\\.ps1 --platform=win32 --arch=x64 --out ./dist --overwrite",
    "test": "echo \"Error: no tests specified\" && exit 1"
  },
  "author": "Marc Fearby",
  "license": "GPL-3.0",
  "dependencies": {
    "electron-routes": "^1.3.0"
  },
  "devDependencies": {
    "electron": "^1.7.6",
    "electron-packager": "^9.0.1"
  }
}
